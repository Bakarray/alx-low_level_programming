#include "main.h"

/**
 * _islower - checks for lowercase character
 *
 * Return: 1 (lowercase), 0(uppercase)
 */

int _islower(int c)
{
	if (char 'c' < 123 && char 'c' > 96)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
