#include <stdio.h>

/**
 * _islower - checks for lowercase character
 *
 * Return: 1 (lowercase), 0(uppercase)
 */

int main(int c)
{
	c = 'c';

        if (c < 123 && c > 96)
        {
                printf("%d", 1);
        }
        else
        {
                printf("%d", 0);
        }
}
