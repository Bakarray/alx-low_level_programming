#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int start = 0;

	for (; start <= 10; start++)
	{
		printf("%d", start);
	}
	printf("\n");

	return (0);
}
